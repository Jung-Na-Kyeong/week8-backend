spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: week8
  profiles:
    active: local

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

cors:
  allowed-origin: ${FRONT_URL}